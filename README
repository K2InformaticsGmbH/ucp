ucperl
======
An Erlang UCP SMS library.

It allows to start an UCP session over a tcp connection.
The ucp_session module define a behaviour for an UCP session.
ucp_syntax and ucp_arg_syntax define the syntax and parameter syntax for EMI/UCP messages.

An example of usage of the library can be find in ucp_example.